# Custom partition table for the Prince of Persia port.
# 4 MB flash. The app embeds the PoP DAT files (~414 KB) plus the pre-decoded
# sprite blob (~472 KB), so the default 1 MB single-app partition is too small.
# Give the factory app 3 MB, which comfortably fits code + embedded assets.
#
# Name,     Type, SubType, Offset,   Size
nvs,        data, nvs,     0x9000,   0x6000
phy_init,   data, phy,     0xf000,   0x1000
factory,    app,  factory, 0x10000,  0x300000
